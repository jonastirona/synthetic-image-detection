You will see three labeled example images followed by a test image to classify.

Example 1 — real: an authentic photograph taken by a camera.
Example 2 — ai_generated: an image produced entirely by a generative AI model.
Example 3 — deepfake: an image where a face or likeness has been digitally manipulated.

Now classify the test image as one of: real, ai_generated, or deepfake.

{output_format}
